{
  "name": "notes-app-js-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle --outfile=bundle.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.18.10",
    "jest-environment-jsdom": "^29.5.0",
    "jest-fetch-mock": "^3.0.3"
  }
}
